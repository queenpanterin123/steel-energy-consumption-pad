---
title: "steel-energy-consumption-pad"
author: "queenpanterin2233jol, agens-a"
format: html
editor: visual
---

## Tytuł

------------------------------------------------------------------------

## EDA

------------------------------------------------------------------------

```{r}
library(corrplot)
data<-read.csv("steel_data.csv")
data$Load_Type <- factor(data$Load_Type, levels = c("Light_Load","Medium_Load","Maximum_Load")) #zmiana na factor ze stringa
#licznosc klas
table(data$Load_Type) #jest ok
dane_numeryczne <- data[, sapply(data,is.numeric)]

#robimy korelacje spearmana - nieliniowa i odporna 
macierz_korelacji_s <- cor(dane_numeryczne, method = "spearman")
round(macierz_korelacji_s, 2) 
#wizual korelacji
corrplot(macierz_korelacji_s,tl.col = "black",tl.cex=0.5,title = "Korelacja Spearmana między predyktorami",mar=c(0,0,2,0))
```

------------------------------------------------------------------------

```{r}
#przygotowanie danych treningowych i testowych
library(rsample)
set.seed(238)
data_split <- initial_split(data, prop = 0.7, strata = Load_Type)
train_data <- training(data_split)
test_data <- testing(data_split)



#analiza glownych skladowych (PCA)
train_num<-train_data[,sapply(train_data,is.numeric)]
test_num<-test_data[,sapply(train_data,is.numeric)]

pca_model <- prcomp(train_num, center = TRUE, scale. = TRUE)
summary(pca_model)
plot(pca_model, type = "lines", main="Wykres osypiskowy")
#sprawdzamy czy jest kwadratowa (macierz ladunkow) - OK
nrow(pca_model$rotation) == ncol(pca_model$rotation)

#zabezpieczamy numerycznie
#pca_model$rotation %*% t(pca_model$rotation) |> round()

#sprawdzamy czy dane sa ustandaryzowane - sumujemy ich odchylenia standardowe, a powinny wynosic tyle ile jest atrybutow 
sum(pca_model$sdev^2)

library(factoextra)
fviz_pca_biplot(pca_model,geom.ind="point",repel=TRUE,legend.title="Poziom ryzyka",title="PCA Biplot - Struktura zbioru treningowego")
```

Redukcja wymiarow

```{r}
#OD ANI: pca zalezy od modelu !!! nie możemy zrobić chyba jednego pca i wtedy pojedyńczo modele
#watpliwosci mamy co do liczby skladowych 4-nieparzyscie; 5-szum
liczba_skladowych<-4

train_pca <- data.frame(pca_model$x[,1:liczba_skladowych])
train_pca$Load_Type <- train_data$Load_Type

test_pca_values <- predict(pca_model, newdata = test_num)
test_pca <- data.frame(test_pca_values[, 1:liczba_skladowych])
test_pca$Load_Type <- test_data$Load_Type


```

**kNN**

```{r}
library(ggplot2)
library(rsample)
library(tidymodels)
library(kknn)
library(dplyr) # Dodajemy pakiet do manipulacji danymi

# 1. Wczytanie i przygotowanie danych
data <- read.csv("steel_data.csv")
data$Load_Type <- factor(data$Load_Type, levels = c("Light_Load", "Medium_Load", "Maximum_Load"))

# --- POPRAWKA: Filtrujemy dane ---
# Zostawiamy wyłącznie kolumny numeryczne oraz zmienną celu (Load_Type).
# Dzięki temu algorytm kNN nie dostanie tekstów, których nie potrafi przetworzyć.
data <- data %>% 
  select(where(is.numeric), Load_Type)

# 2. DOWNSAMPLING 
set.seed(238)
data_sampled_split <- initial_split(data, prop = 0.5, strata = Load_Type)
data_small <- training(data_sampled_split)

# 3. Właściwy podział na zbiór treningowy (70%) i testowy (30%) z mniejszego zbioru
ostateczny_split <- initial_split(data_small, prop = 0.7, strata = Load_Type)
train_data <- training(ostateczny_split)
test_data <- testing(ostateczny_split)

# 4. Utworzenie foldów do kroswalidacji na nowym zbiorze treningowym
set.seed(123)
foldy <- vfold_cv(train_data, v = 5)

# 5. Definicja modelu kNN (tunujemy liczbę sąsiadów i funkcję wagową)
knn_model <- nearest_neighbor(
  neighbors = tune(),
  weight_func = tune()
) %>%
  set_engine("kknn") %>%
  set_mode("classification")

# 6. Recepta z optymalizacją PCA
# Normalizacja jest kluczowa przed wykonaniem PCA
knn_pca_recipe <- recipe(Load_Type ~ ., data = train_data) %>%
  step_normalize(all_numeric_predictors()) %>%
  step_pca(all_numeric_predictors(), num_comp = tune())

# 7. Połączenie modelu i recepty w Workflow
knn_workflow <- workflow() %>%
  add_model(knn_model) %>%
  add_recipe(knn_pca_recipe)

# 8. Siatka hiperparametrów (Grid)
# Sprawdzamy bezpieczny zakres, który nie wysadzi pamięci RAM
knn_grid <- expand.grid(
  neighbors = c(3, 5, 7, 9, 11, 13),
  weight_func = c("rectangular", "gaussian"),
  num_comp = c(3, 4, 5)
)

# 9. Tuning (Kroswalidacja)
# Na zmniejszonym zbiorze powinno policzyć się szybko nawet na jednym rdzeniu
knn_tune_res <- tune_grid(
  knn_workflow,
  resamples = foldy,
  grid = knn_grid,
  metrics = metric_set(accuracy, kap)
)

# 10. Wizualizacja wyników kroswalidacji (wykres do raportu)
wykres_knn <- knn_tune_res %>%
  collect_metrics() %>%
  filter(.metric == "accuracy") %>%
  ggplot(aes(x = neighbors, y = mean, color = as.factor(num_comp))) +
  geom_line(linewidth = 1, alpha = 0.8) +
  geom_point(size = 2) +
  facet_wrap(~ weight_func) +
  theme_linedraw() +
  theme(legend.position = "bottom") +
  labs(
    title = "Optymalizacja modelu kNN połączona z PCA",
    x = "Liczba sąsiadów (k)",
    y = "Dokładność (Accuracy)",
    color = "Liczba składowych PCA:"
  )

print(wykres_knn)

# 11. Wyciągnięcie najlepszych parametrów i ostateczny test
best_knn <- select_best(knn_tune_res, metric = "accuracy")
print(best_knn) # Pokazuje w konsoli, co wygrało

# Finalizacja workflow najlepszymi parametrami
final_knn_wf <- finalize_workflow(knn_workflow, best_knn)

# Uczenie ostatecznego modelu na całym zbiorze treningowym i test na testowym
final_knn_fit <- last_fit(final_knn_wf, split = ostateczny_split)

# Wyświetlenie końcowych wyników metryk na nowym zbiorze testowym!
wyniki_testowe_knn <- collect_metrics(final_knn_fit)
print(wyniki_testowe_knn)



```

```{r}
# Wyciągamy predykcje ze zbioru testowego
predykcje_knn <- collect_predictions(final_knn_fit)

# Generujemy macierz pomyłek
macierz_knn <- conf_mat(predykcje_knn, truth = Load_Type, estimate = .pred_class)

# Zapisujemy metryki kNN na później, dodając kolumnę z nazwą modelu
podsumowanie_knn <- wyniki_testowe_knn %>%
  select(.metric, .estimate) %>%
  mutate(model = "kNN + PCA")

# Rysujemy estetyczny wykres (tzw. heatmapę)
autoplot(macierz_knn, type = "heatmap") +
  scale_fill_gradient(low = "#e5f5f9", high = "#2ca25f") +
  theme_linedraw() +
  labs(
    title = "Macierz pomyłek - Model kNN",
    subtitle = "Zestawienie klas rzeczywistych i przewidywanych"
  )


```

```{r}
# Wyciągamy szczegółowe predykcje z modelu
predykcje_knn <- collect_predictions(final_knn_fit)

# Zaktualizowana reguła biznesowa: eliminacja skrajnych błędów
predykcje_bezpieczne_v2 <- predykcje_knn %>%
  mutate(
    pred_biznesowa = case_when(
      # 1. Jeśli jest przynajmniej 30% szans na Maximum -> od razu zakładamy Maximum
      .pred_Maximum_Load >= 0.30 ~ "Maximum_Load",
      
      # 2. BUFOR BEZPIECZEŃSTWA: 
      # Jeśli szansa na Maximum jest zauważalna (np. > 10%) LUB szansa na Medium > 35%
      # przypisujemy Medium_Load. To blokuje wrzucenie obserwacji do Light_Load!
      .pred_Maximum_Load >= 0.10 | .pred_Medium_Load >= 0.35 ~ "Medium_Load",
      
      # 3. Light_Load wybieramy TYLKO wtedy, gdy model jest absolutnie pewny, 
      # że ryzyko większego obciążenia jest marginalne.
      TRUE ~ "Light_Load"
    )
  ) %>%
  mutate(pred_biznesowa = factor(pred_biznesowa, levels = levels(Load_Type)))

# Generujemy nową macierz pomyłek
macierz_biznesowa_v2 <- conf_mat(predykcje_bezpieczne_v2, truth = Load_Type, estimate = pred_biznesowa)

# Rysujemy
autoplot(macierz_biznesowa_v2, type = "heatmap") +
  scale_fill_gradient(low = "#f2f0f7", high = "#54278f") +
  theme_linedraw() +
  labs(
    title = "Biznesowa Macierz Pomyłek - kNN (Wersja 2.0)",
    subtitle = "Zabezpieczenie przed skrajnym niedoszacowaniem obciążenia"
  )

```
